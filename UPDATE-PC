#!/bin/sh

tar -c -f - \
    --exclude '*~' \
    --exclude '*.orig' \
    --exclude '.gitignore' \
    FlyingBrick.blend \
    Project/PackageSources \
    Modules/FlyingBrick/FlyingBrick/*.cpp Modules/FlyingBrick/FlyingBrick/*.h \
    | \
    (cd /Volumes/stinky/Users/tml/OneDrive/flying-brick && tar xvf -)
